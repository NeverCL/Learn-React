# 常见问题

## 如何调试React

## 引用antd首次打开速度太慢

1. 按需加载
1. 使用cdn(需要引用moment.js)

```js
module.exports = {
    mode: 'production',
    devtool: 'source-map',
    module: {
        rules: [{
            test: /\.(js|jsx)$/,
            loader: 'babel-loader',
            options: {
                presets: ['react'],
                plugins: [
                    ["import", { "libraryName": "antd", "style": "css" }]
                ]
            },
            exclude: /node_modules/
        }]
    },
}
```

```js
module.exports = {
    mode: 'development',
    devtool: 'inline-source-map',
    externals: {
        lodash: '_',
        react: 'React',
        'react-dom': 'ReactDOM',
        antd: 'antd'
    },
}
```

`npx webpack`