# 常见问题

## 引用antd首次打开速度太慢

```js
module.exports = {
    mode: 'production',
    devtool: 'source-map',
    module: {
        rules: [{
            test: /\.(js|jsx)$/,
            loader: 'babel-loader',
            options: {
                presets: ['react'],
                plugins: [
                    ["import", { "libraryName": "antd", "style": "css" }]
                ]
            },
            exclude: /node_modules/
        }]
    },
}
```

`npx webpack`